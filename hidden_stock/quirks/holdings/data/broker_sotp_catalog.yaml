# Production catalog: intentionally empty for a public repo.
# Copy broker_sotp_catalog.example.yaml → this file (or merge series) and
# point `url` at broker PDFs you are licensed to use. Never commit paid
# research PDFs or live sell-side URLs with proprietary tables.
#
# SERIES RULE: when you add one report, backfill prior vintages from the
# same broker×parent into the same series so stake % can be confirmed over time.

series: []
